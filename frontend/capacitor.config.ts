import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.velmoria',
  appName: 'Velmoria',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
